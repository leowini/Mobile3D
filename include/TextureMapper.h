#ifndef TEXTURE_MAPPER_H
#define TEXTURE_MAPPER_H

#include "Keyframe.h"
#include "Map.h"
#include <vector>

using namespace std;

class TextureMapper {
public:
  TextureMapper(int mesh, Map &map);
};

#endif
